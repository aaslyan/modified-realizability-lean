% Generated verbatim from `#realizer` output (scratchpad/xray.lean); do not
% hand-edit. Each line is one tabular row; glyphs map to math ($\cdot$,
% $\Lambda$, $\prec$, $\langle$, ...) and every space becomes ~ so the
% monospace tree indentation is reproduced exactly. Rows end with \\.
exI~~$\langle$witness~=~5,~$\cdot$$\rangle$ \\
~~~$\cdot$~$\langle$contentless$\rangle$~~good(3,5)~=~0
